Config = {}
Config.Locale = 'en'
Config.Visible = true

Config.Food = {
	["bread"] = {
		status = 10000,
		remove = false -- want the item to remove on use
	}
}

Config.Drinks = {
	["water"] = {
		status = 10000,
		remove = true  -- want the item to remove on use
	}
}
